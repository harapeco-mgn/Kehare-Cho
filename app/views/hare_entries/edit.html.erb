<main class="max-w-3xl mx-auto p-4 md:p-8 font-zen">
  <%# 戻るリンク %>
  <div class="mb-4">
    <%= link_to hare_entry_path(@hare_entry), class: "inline-flex items-center gap-1 text-[#8D7B68] hover:text-[#C87941] font-medium transition-colors" do %>
      <%= app_icon("arrow-left", css_class: "w-4 h-4") %> 詳細に戻る
    <% end %>
  </div>

  <%# ページヘッダー %>
  <div class="mb-6">
    <h1 class="text-2xl md:text-3xl font-bold text-[#4A4036] mb-1 tracking-wide">ハレの記録を編集する</h1>
    <p class="text-[#8D7B68] flex items-center gap-1 text-sm">
      記録を更新しましょう
      <%= app_icon("notebook-pen", css_class: "w-4 h-4 text-[#8D7B68]") %>
    </p>
  </div>

  <%# フォームカード %>
  <div class="bg-[#FFFCF5] rounded-3xl shadow-sm border border-[#E8E0D0] p-6 md:p-8">
    <%= render 'form', hare_entry: @hare_entry, submit_label: "更新する", cancel_path: hare_entry_path(@hare_entry), hare_tags: @hare_tags %>
  </div>
</main>
